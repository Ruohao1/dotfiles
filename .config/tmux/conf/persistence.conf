# Checkpoint only after explicit confirmation. Automatic restore has no key binding.
bind-key s confirm-before -p "Save tmux workspace now? (y/n)" \
  "run-shell '$HOME/.config/tmux/scripts/workspace save'"
